<nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom sticky-top shadow-sm">
  <div class="container">
    <!-- Brand -->
    <a class="navbar-brand fw-bold d-flex align-items-center" href="/">
      <i class="fa-solid fa-house-chimney me-2" style="color: #ef7d44; font-size: 1.5rem;"></i>
      <span style="color: #2c3e50; font-size: 1.4rem;">Homify</span>
    </a>

    <!-- Toggler for mobile -->
    <button class="navbar-toggler" type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarNav"
            aria-controls="navbarNav"
            aria-expanded="false"
            aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Navbar links -->
    <div class="collapse navbar-collapse" id="navbarNav">

      <!-- Left side links -->
      <div class="navbar-nav ms-0 ms-lg-4">
        <a class="nav-link d-flex align-items-center" href="/">
          <i class="fa-solid fa-home me-2"></i>Home
        </a>
        <a class="nav-link d-flex align-items-center" href="/listings">
          <i class="fa-solid fa-list me-2"></i>All Listings
        </a>
        <% if (currUser) { %>
          <a class="nav-link d-flex align-items-center" href="/listings/new">
            <i class="fa-solid fa-plus me-2"></i>Add Listing
          </a>
        <% } %>
      </div>

      <!-- Right side links -->
      <div class="navbar-nav ms-auto">
        <% if (!currUser) { %>
          <!-- Not logged in -->
          <a class="nav-link" href="/signup">
            <i class="fa-solid fa-user-plus me-1"></i>Sign Up
          </a>
          <a class="nav-link" href="/login">
            <i class="fa-solid fa-sign-in-alt me-1"></i>Log In
          </a>
        <% } else { %>
          <!-- Logged in -->
          <div class="nav-item dropdown">
            <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown">
              <i class="fa-solid fa-user-circle me-1"></i><%= currUser.username %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
              <li><a class="dropdown-item" href="/listings"><i class="fa-solid fa-list me-2"></i>My Listings</a></li>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item text-danger" href="/logout"><i class="fa-solid fa-sign-out-alt me-2"></i>Log Out</a></li>
            </ul>
          </div>
        <% } %>
      </div>

    </div>
  </div>
</nav>
